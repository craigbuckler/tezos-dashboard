{
  "name": "tezos-fetchreduce",
  "version": "0.1.0",
  "description": "Tezos data fetching APIs for dashboard.",
  "type": "module",
  "main": "tasks.js",
  "scripts": {
    "debug": "rm -f ./tasks.log && node ./update.js && node ./tasks.js",
    "start": "node --no-warnings ./update.js && node --no-warnings ./tasks.js"
  },
  "author": "Craig Buckler",
  "license": "MIT",
  "dependencies": {
    "dotenv": "16.0.1",
    "mongodb": "4.8.0"
  },
  "devDependencies": {
    "nodemon": "2.0.19"
  }
}
